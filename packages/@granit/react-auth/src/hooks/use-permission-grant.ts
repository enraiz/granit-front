// Facade re-export — prefer @granit/react-authorization directly
export { usePermissionGrant } from '@granit/react-authorization';
export type { UsePermissionGrantReturn } from '@granit/react-authorization';
