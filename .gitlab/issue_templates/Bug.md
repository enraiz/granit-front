## Description du bug

Description claire et concise du probleme.

## Package concerne

`@granit/logger` | `@granit/types` | `@granit/utils` | `@granit/api-client` | `@granit/auth`

## Etapes pour reproduire

1. Importer `...` depuis `@granit/...`
2. Appeler `...` avec les parametres `...`
3. Voir l'erreur

## Comportement attendu

Ce qui devrait se passer.

## Comportement actuel

Ce qui se passe reellement.

## Environnement

- **Version @granit** :
- **App consommatrice** (guava-front / guava-admin) :
- **Node.js** :
- **TypeScript** :

## Logs / Erreurs console

```text
Coller les logs ici
```

## Severite

- [ ] Critique (bloquant, build casse)
- [ ] Majeur (feature cassee, workaround difficile)
- [ ] Mineur (genant mais contournable)
- [ ] Cosmetique (types incorrects, avertissements)

/label ~"Type::Bug" ~"Priority::High"
